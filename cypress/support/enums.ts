export enum Status {
	Checked = 'checked',
	Completed = 'completed',
    Uncompleted = '',
}

export enum TodoList {
	Title = 'todos',
    Placeholder = 'What needs to be done?'
}

export enum Filters {
	Active = 'active',
    Complete = 'complete'
}
